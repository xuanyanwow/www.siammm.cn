# mysql 锁


## 角度一：级别

### 全局锁
### 表级锁
### 页级锁
### 间隙锁
### 行级锁


## 角度二：加锁策略

### 乐观锁

假的 不是mysql的  而是用户自己设计

### 悲观锁

真实的mysql锁，把资源lock住，不让其他的`更改`


## 角度三：悲观锁共享类型

### 共享锁（读锁）

- 事务1锁住
- 事务2不能更新 可以读（读的时候不一定要加锁）
- 事务3要更新 就要等事务1/2的锁全部释放才可以更新

### 排它锁（写锁）

- 其他事务也不能获取到写锁 默认增删改都会加写锁 自动


## 其他角度

### 自增锁

如果表存在自增字段,则mysql会额外增加一个自增锁用于控制自增数.

自增锁的配置 innodb_autoinc_lock_mode 3种模式

0:traditonal 表锁,每次都会产生表锁

1:consecutive 轻量锁,innodb默认,正常插入获得批量的锁,可以保证连续插入

2:interleaved 不锁表,当在主从同步时,可能产生数据不一致的问题

