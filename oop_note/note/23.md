# 享元模式

> 运用共享技术有效地支持大量细粒度的对象

- 连接池、线程池
- 游戏客户端的元素池


- 如果一个应用程序使用了大量的对象，而大量的这些对象造成了很大的存储开销时就应该考虑使用；
- 对象的大多数状态可以外部状态，如果删除对象的外部状态，那么可以用相对较少的共享对象取代很多组对象，此时可以考虑享元模式
- 享元模式需要维护一个`记录了系统已有的所有享元的列表，本身是需要消耗资源的`，并且使得系统更加复杂，逻辑复杂化，只要足够多的对象实例可供共享时才值得使用。