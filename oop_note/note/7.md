# 原型模式

> 用原型实例指定创建对象的种类，并且通过拷贝这些原型创建新的对象

- 省去构造函数的执行过程，直接克隆值
- 一般在初始化的信息不不发生变化的情况下使用，隐藏了对象创建的细节，对性能有提高


## 浅复制和深复制

浅复制 > 字段是引用类型，则复制引用但不复制引用的对象。造成原始对象和复本引用同一对象


思考用途
- 游戏服务端时，生成固定场景的地图，可以在服务端启动的时候生成好，其他玩家加入的时候 拷贝克隆初始版本使用即可