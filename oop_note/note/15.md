# 备忘录模式

> 在不破坏封装性的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态，这样以后就可将该对象恢复到原先保存的状态。

- Originator.createMementor -> Mementor[备忘录类] -> Caretaker[保存备忘录] -> Originator.setMementor
- 下棋时悔棋、编写文档的撤销、查看网页时需要后退 
- 功能比较复杂的，需要维护或记录属性历史的类；需要保存的属性只是众多属性的一小部分时，Originator可以根据保存的Memento信息还原到前一状态。

