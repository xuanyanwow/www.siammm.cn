# 迭代器模式


> 提供一个方法顺序访问一个聚合对象中各个元素，而又不暴露该对象的内部表示

- 当你需要对聚集有多种方式遍历时，可以考虑用迭代器模式
- 各大语言中都已经内置了迭代器抽象 
- 迭代器分离了对象的遍历行为，抽象出一个迭代器类来负责，可以做到不暴露集合的内部结构，又可以让外部代码透明地访问集合内部的数据
- 由于它太常用了，各大语言都对它进行了封装，所以给人感觉不太常用`其实语法就是默默在使用了`