# 代理模式

> 为其他对象提供一种代理以控制这个对象的访问

- 远程代理，为一个对象在不同的地址空间提供局部代表，隐藏对象存在于不同地址空间的事实
- 虚拟代理，根据需要创建开销很大的对象。通过它来存放实例化需要很长时间的真实对象（浏览器里的图片和音频  用代理对象 框框代替 慢慢下载）
- 安全代理，控制真实对象访问时的权限。用于对象应该有不同的访问权限的时候
- 智能指引，调用真实的对象时，代理处理另外一些事。如计算真实对象的引用次数；或者当第一次引用一个持久对象时，将它装入内存；或者在访问一个对象前，检查是否已经锁定它，以确保其他对象不能改变


## 应用场景

1.日志的采集
2.权限控制
3.实现aop
4.Mybatis mapper
5.Spring的事务
6.全局捕获异常
7.Rpc远程调用接口
8.分布式事务原理代理数据源


## 静态代理和动态代理

静态代理在使用时,需要定义接口或者父类,被代理对象与代理对象一起实现相同的接口或者是继承相同父类
- 1.可以在不修改代理目标对象的前提下，对代理目标的功能进行拓展。
- 2.需要实现代理目标对象实现的接口，一旦代理目标所实现的接口有修改，目标对象与代理都需要维护。

动态代理

- 1. 代理对象,不需要实现接口
- 2. 代理对象的生成,是利用反射实现，动态的在内存中构建代理对象

```php
//代理的目标类
class Cooperation{
    public function execute($data) {
        echo $data[0]."数据处理过程";
    }
}
//代理类
class Proxy{
    private $target;
    //将目标对象通过构造函数注入
    public function __construct($tar){
        $this->target[] = new $tar;
    }
    //调用一个自己不存在方法时调用，魔术方法
    public function __call($name,$args){
        foreach ($this->target as $obj) {
            $ref = new ReflectionClass($obj);
            if($method = $ref->getMethod($name)){
                if($method->isPublic()&&!$method->isAbstract()){
                    echo "前置操作";
                    $method->invoke($obj,$args);
                    echo "后置操作";
                }
            }
        }
    }
}
测试用例
$proxy = new Proxy('Cooperation');
$proxy->execute('动态');
```