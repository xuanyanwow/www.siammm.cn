# 适配器模式

> 将一个类的接口转换成客户希望的另外一个接口。Adapter模式使得原本由于接口不兼容而不能一起工作的那些类可以一起工作。

- 复用已经存在的旧接口时
- 两个类所做的事情相同或相似，但是具有不同的接口时
- 通常是在软件开发后期或维护期使用适配器模式（双方都不太容易修改的时候）
- 数据转换时可以用，比如可能得到的是SQL SERVER的数据源 也可能得到的是MYSQL SERVER的数据源，可以用DataAdatapter来得到统一的 数组类型