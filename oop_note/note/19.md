# 桥接模式

## 合成/聚合复用原则

> 尽量使用合成/聚合，尽量不要使用类继承


## 桥接模式

> 将抽象部分与它的实现部分分离，使它们都可以独立地变化

```
手机品牌        <------------------->    手机软件
> 手机品牌N                              > 通讯录
> 手机品牌M                              > 游戏
```


- 实现系统可能有多角度分类，每一种分类都可能变化，那么就把这种多角度分离出来让它们独立变化，减少它们之间的耦合。

JDBC
JDBC连接数据库时：利用驱动来桥接
JDBC连接数据库时，在各个数据库之间进行切换，基本不需要动太多的代码，甚至丝毫不动，原因就是JDBC提供了统一接口，每个数据库提供各自的实现，用一个叫做数据库驱动的程序来桥接就行了

## 思考

订单类型：赠送、普通
支付方式：微信、支付宝

结算时，组合起来，走结算逻辑

```php
interface Order {
    public $type;
    // 演示代码 不完整
    public $payType;
    
    public function setPayType(PayType $payType);
    
    public function settle();
}

interface PayType {
}


class NormalOrder implements Order
{
    pubcli $type = 'normal';
    
    
    public function settle(){
        echo $this->payType->pay_type_name . "结算普通订单";
        // 更改订单状态，调用payType类的settle逻辑去扣款
    }
}

class SendOrder implements Order
{
    pubcli $type = 'send';
    
    public function settle(){
        echo $this->payType->pay_type_name . "结算赠送订单";
    }
}

```